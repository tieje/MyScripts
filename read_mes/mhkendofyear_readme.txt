- python 3.7
- run "pip install xlwings"
	- xlwings==0.16.4

I kinda regret writing this script since it's application is so specialized to end of year activities for writing Scopes. It wasn't until I finished writing this script that I realized that I could have achieved the same effect by concatenating strings in Excel. I felt so dumb after writing this.